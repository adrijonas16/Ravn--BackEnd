Run a security check for: $ARGUMENTS

Use the security-check skill. Check auth guards, role decorators, data exposure in responses, input validation, and frontend token handling. Report vulnerabilities and recommended fixes.
