Check if documentation needs updates for: $ARGUMENTS

Use the docs-sync skill. Compare docs against the implementation and tests. Update only directly affected documentation. Report any outdated docs outside the current scope.
