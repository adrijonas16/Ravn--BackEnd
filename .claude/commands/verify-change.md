Verify the following change with executable checks: $ARGUMENTS

Use the verify-change skill. Run targeted checks first, then broader checks. Produce a verification report with evidence, mock boundaries, limitations, and a recommended commit message.
