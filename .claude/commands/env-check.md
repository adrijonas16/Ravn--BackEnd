Check environment configuration for: $ARGUMENTS

Use the env-check skill. Compare .env against .env.example, validate variable formats without exposing secrets, test service connectivity, and report missing or misconfigured variables.
